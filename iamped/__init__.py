"""iAmped — build iPod / MP3-player libraries from Plexamp (Plex) metadata.

The app reads play counts, ratings and playlists from a Plex Media Server,
caches the metadata locally, downloads the actual audio over the Plex API, and
fills a target device (classic iPod or USB mass-storage player) to its capacity.
"""

__version__ = "0.1.0"
